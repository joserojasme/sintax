import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#121f3d',
      contrastText: '#fff',
    },
    secondary: {
      main: '#484848',
    },
    error: {
      main: '#F44336',
    },
  },
});
